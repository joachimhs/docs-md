spec_version: "0.1.0"
project:
  name: "docs.md"
  description: "Documentation that is Git-able, lives with your codebase, easy for humans and agents to maintain via the IDE, CLI, or web interface."
auth:
  enabled: true
  mode: simple
  simple:
    session_secret: "testpassword"
hosting:
  auto_pull: true
  auto_pull_interval: 30
